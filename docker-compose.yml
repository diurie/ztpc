version: "3.8"

services:
  http:
    build: ./http
    container_name: web
    volumes:
      - "./http/content:/var/www/html:ro"
      - "./http/conf/nginx.conf:/etc/nginx/nginx.conf:ro"
      - "./http/log:/var/log/nginx"
    ports:
      - target: 80
        published: 8080